export enum LAYOUTS {
  default = 'default',
  withoutHeader = 'withoutHeader',
}

export const LAYOUTS_MAP: Record<LAYOUTS, string> = {
  default: 'DefaultLayout',
  withoutHeader: 'WithoutHeaderLayout',
}
